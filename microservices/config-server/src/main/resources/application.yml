server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: native
  cloud:
    config:
      server:
        native:
          # Serves config from the classpath /config folder (git-backed is the prod alternative).
          search-locations: classpath:/config
