server:
  port: 8761

spring:
  application:
    name: discovery-server

# A standalone registry doesn't register with (or fetch from) itself.
eureka:
  client:
    register-with-eureka: false
    fetch-registry: false
  instance:
    hostname: ${EUREKA_HOSTNAME:localhost}
